# Backend - Event Booking System

Quick setup:
1. `npm install`
2. `npm run seed` (to add sample events and seats)
3. `npm run dev`

Runs on port 5000 by default.

.env has the DB and JWT stuff already set up.

Built with Express, MongoDB, Mongoose.
